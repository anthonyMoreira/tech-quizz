# Quickstart

This feature does not introduce any new backend services or third-party infrastructure. All data storage relies on standard browser `localStorage`. 

To reset your testing environment during development, simply clear the `tech_quizz_history` key from the browser's Local Storage, or run `localStorage.clear()` in the dev tools console.
